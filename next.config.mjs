/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','img.icons8.com','avatars.githubusercontent.com'],
    }
};

export default nextConfig;
